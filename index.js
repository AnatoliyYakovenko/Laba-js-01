const a = +prompt("Введіть число:");
const b = +prompt("Введіть друге число:");

const sum = a + b;
const difference = a - b;
const multiply = a * b;
const division = a / b;

const result =
  "сума: " +
  sum +
  "\nрізниця: " +
  difference +
  "\nдобуток: " +
  multiply +
  "\nчастка: " +
  division;

alert(result);
